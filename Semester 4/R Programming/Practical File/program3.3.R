# Q3.3 WAP to get the structure of the given dataframe structure

# Given dataframe
df <- data.frame(
  ID = c(1, 2, 3),
  Name = c("John", "Alice", "Bob"),
  Age = c(30, 25, 35),
  Married = c(TRUE, FALSE, TRUE),
  Salary = c(50000, 60000, 55000)
)

# Get the structure of the dataframe
str(df)
